[phases.setup]
nixPkgs = ["nodejs_20", "chromium", "nss", "nspr", "atk", "cups", "dbus", "expat", "glib", "mesa", "libxkbcommon", "pango", "cairo", "alsa-lib"]

[phases.build]
cmds = ["npm ci --ignore-scripts"]

[start]
cmd = "node src/index.js"
